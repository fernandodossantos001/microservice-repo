spring:
  datasource:
    username: root
    password: my-secret-pw
    url: jdbc:mysql://localhost:3306/transportador
  jpa:
    hibernate:
      ddl-auto: update
      format-sql: true
    show-sql: true
